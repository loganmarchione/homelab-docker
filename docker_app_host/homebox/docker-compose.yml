services:
  homebox:
    container_name: homebox
    image: ghcr.io/sysadminsmedia/homebox:0.21.0-rootless@sha256:572a356f3c18b880feb83987aad1c32c9472fd3212f02312342451fee78792c0
    restart: unless-stopped
    environment:
      - TZ=America/New_York
    networks:
      - proxy
    volumes:
      - 'homebox_data:/data'
    labels:
      - traefik.enable=true
      - traefik.http.routers.homebox.rule=Host(`homebox.${SECRET_INTERNAL_DOMAIN_NAME}`)
      - traefik.http.routers.homebox.tls=true
      - traefik.http.routers.homebox.tls.certresolver=cloudflare
      - traefik.http.routers.homebox.entrypoints=websecure
      - traefik.http.services.homebox.loadbalancer.server.port=7745

networks:
  proxy:
    external: true

volumes:
  homebox_data:
    driver: local
