services:
  webdav:
    container_name: webdav
    image: loganmarchione/docker-webdav-nginx:1.0.2@sha256:4fc1c4b87d79bfe5474fa6f4ce96c94ea607f4ecafb268da3c8edf0dd279d399
    restart: unless-stopped
    env_file:
      - /custom/files/webdav.env
    networks:
      - proxy
    volumes:
      - 'webdav:/var/www/webdav'
    labels:
      - traefik.enable=true
      - traefik.http.routers.webdav.rule=Host(`webdav.${SECRET_INTERNAL_DOMAIN_NAME}`)
      - traefik.http.routers.webdav.tls=true
      - traefik.http.routers.webdav.tls.certresolver=cloudflare
      - traefik.http.routers.webdav.entrypoints=websecure
      - traefik.http.services.webdav.loadbalancer.server.port=80

networks:
  proxy:
    external: true

volumes:
  webdav:
    driver: local
