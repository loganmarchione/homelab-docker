services:
  dokuwiki:
    container_name: dokuwiki
    image: php:8.5-apache-bookworm@sha256:c7c967859d6c4e4d3d215e325048b0678d3a44441507bfb5dbdd6c4bcac7c442
    restart: unless-stopped
    environment:
      - TZ=America/New_York
    networks:
      - proxy
    volumes:
      - 'dokuwiki_config:/var/www/html'
      - 'php_config:/usr/local/etc/php'
    labels:
      - traefik.enable=true
      - traefik.http.routers.wiki.rule=Host(`wiki.${SECRET_INTERNAL_DOMAIN_NAME}`)
      - traefik.http.routers.wiki.tls=true
      - traefik.http.routers.wiki.tls.certresolver=cloudflare
      - traefik.http.routers.wiki.entrypoints=websecure
      - traefik.http.services.wiki.loadbalancer.server.port=80

networks:
  proxy:
    external: true

volumes:
  dokuwiki_config:
    driver: local
  php_config:
    driver: local
