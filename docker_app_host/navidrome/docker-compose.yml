services:
  navidrome:
    container_name: navidrome
    image: ghcr.io/navidrome/navidrome:0.62.0@sha256:c4b5cb36a790b3eb63ca6a68bbe2fe149c2d7fa2e586f7a480e61db630e6664b
    restart: unless-stopped
    environment:
      - TZ=America/New_York
    env_file:
      - /custom/files/navidrome.env
    networks:
      - proxy
    volumes:
      - 'navidrome_data:/data'
      - '/custom_mounts/media/library/Music:/music:ro'
    labels:
      - traefik.enable=true
      - traefik.http.routers.navidrome.rule=Host(`navidrome.${SECRET_INTERNAL_DOMAIN_NAME}`)
      - traefik.http.routers.navidrome.tls=true
      - traefik.http.routers.navidrome.tls.certresolver=cloudflare
      - traefik.http.routers.navidrome.entrypoints=websecure
      - traefik.http.services.navidrome.loadbalancer.server.port=4533
    security_opt:
      - no-new-privileges=true
    cap_drop:
      - ALL

networks:
  proxy:
    external: true

volumes:
  navidrome_data:
    driver: local
